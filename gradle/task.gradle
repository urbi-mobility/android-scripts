import urbi.co.CommitVersionsTask
import urbi.co.RemoveGitHubPackage
import urbi.co.UpdateVersionLibTask
import urbi.co.UploadGitHubPackage

tasks.register("remove-package-urbi", RemoveGitHubPackage)
tasks.register("uploadlib", UploadGitHubPackage)
tasks.register("uploadlibskip", UploadGitHubPackage){
    skipService=true
}
tasks.register("commitversions", CommitVersionsTask)
tasks.register("update-version-lib", UpdateVersionLibTask)
